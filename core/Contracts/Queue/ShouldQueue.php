<?php

namespace Core\Contracts\Queue;

/**
 * Interface for jobs/mailables/notifications that should be queued.
 */
interface ShouldQueue
{
    // Can add properties like $connection, $queue, $delay later
}
